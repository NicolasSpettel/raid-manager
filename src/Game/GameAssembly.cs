namespace Game;

/// <summary>
/// Namespace/assembly anchor. Lets other tooling (e.g. the architecture boundary tests) reference
/// the Game assembly before it has real gameplay types. Replace usages as real types land.
/// </summary>
public static class GameAssembly
{
}
