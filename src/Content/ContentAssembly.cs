namespace Content;

/// <summary>
/// Namespace/assembly anchor. Lets other tooling (e.g. the architecture boundary tests) reference
/// the Content assembly before it has real content types. Replace usages as real types land.
/// </summary>
public static class ContentAssembly
{
}
